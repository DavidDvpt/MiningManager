<local:BaseView x:Class="MiningManager.View.ItemManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiningManager.View"
             xmlns:vm="clr-namespace:MiningManager.ViewModel;assembly=MiningManager.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <local:BaseView.Resources>
        <DataTemplate DataType="{x:Type vm:DataGridViewModel}">
            <local:DataGridView/>
        </DataTemplate>
    </local:BaseView.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <DataGrid Name="dgGeneric" 
                      ItemsSource="{Binding ViewData.Finders, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      Margin="5,3"
                      IsReadOnly="True"/>
    </Grid>
</local:BaseView>
