using MiningManager.ViewModel.ControllerInterfaces;

namespace MiningManager.ViewModel
{
    public interface IItemEditViewModel
    {
        void Init(IController controller, int selectedId);
    }
}
